{
  "name": "portfolio",
  "private": true,
  "scripts": {
    "dev": "npm --prefix website run dev",
    "dev:website": "npm --prefix website run dev",
    "dev:studio": "npm --prefix studio run dev",
    "install:website": "npm --prefix website install",
    "install:studio": "npm --prefix studio install",
    "install:all": "npm install && npm --prefix website install && npm --prefix studio install",
    "typegen": "npm --prefix studio run typegen && npm --prefix website exec -- prettier --write sanity.types.ts",
    "lint:all": "npm --prefix website run lint:all && npm --prefix studio run lint",
    "prepare": "husky",
    "build": "npm --prefix website run build"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "prettier": "^3.9.6"
  }
}
